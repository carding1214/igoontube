<Window x:Class="PUPlayer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:PUPlayer.App.Views"
        Title="IgoonTube" Width="1180" Height="720" MinWidth="720" MinHeight="420"
        WindowStartupLocation="CenterScreen" Background="{DynamicResource CanvasBrush}"
        FontFamily="Segoe UI" FontSize="12" UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">
    <!--
    THESIS: dos reproductores realmente independientes; se evita un lanzador y el video ocupa casi toda la ventana.
    OWN-WORLD: consola de reproducción Windows en carbón, texto frío y una señal naranja cálida; controles rectos y compactos.
    STORY: abrir o soltar un archivo, navegarlo y ajustar cada panel sin afectar al otro.
    FIRST VIEWPORT: barra mínima arriba, uno o dos lienzos negros dominantes y controles por panel abajo.
    FORM: utilidad de reproducción familiar fijada por el usuario; code-led; seed 4c4beaa2.
    FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, and DESIGN.md
    -->
    <Border Background="{DynamicResource CanvasBrush}">
        <views:WorkspaceView x:Name="Workspace"/>
    </Border>
</Window>
